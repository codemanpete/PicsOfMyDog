import { connect } from 'react-redux';
import PetPhotos from './pet_photos';

const mapStateToProps = (state) => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(PetPhotos);
